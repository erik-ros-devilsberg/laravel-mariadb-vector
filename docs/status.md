# Sprint Status

Maintained by the agile plugin. One row per sprint — updated by `/agile:shape`, `/agile:execute`.

| Sprint | Slug | Status | Description |
|--------|------|--------|-------------|
| Fix VectorCast Binary Detection | fix-vectorcast-binary-detection | done | Fix 1-in-256 read crash when binary vectors are misdetected as JSON. |
